package com.electronik54.revision_3.corejava.ch159_gradle_basics;

/**
 * Challenge 159: Gradle Basics
 * 
 * Challenge Statement:
 * Understand Gradle build tool fundamentals:
 * - build.gradle (Groovy DSL) vs build.gradle.kts (Kotlin DSL)
 * - Tasks, plugins, dependencies
 * - Gradle Wrapper (gradlew) for reproducible builds
 * - Compare Gradle vs Maven
 * - Multi-module project configuration
 * 
 * Hint:
 * - build.gradle.kts uses Kotlin DSL (preferred for new projects)
 * - tasks are similar to Maven phases but more flexible
 * - Gradle uses incremental builds (faster than Maven)
 * - gradlew and gradlew.bat bootstrap Gradle without installation
 * 
 * Expected Output (Solution):
 * Gradle vs Maven comparison:
 * build.gradle vs pom.xml (configuration)
 * tasks vs phases (execution model)
 * incremental vs full build (performance)
 * Kotlin DSL vs XML (expressiveness)
 * gradlew vs mvnw (wrapper)
 */
public class Challenge {

}