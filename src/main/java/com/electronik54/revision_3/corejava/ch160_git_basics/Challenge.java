package com.electronik54.revision_3.corejava.ch160_git_basics;

/**
 * Challenge 160: Git Basics
 * 
 * Challenge Statement:
 * Understand Git version control fundamentals:
 * - Repository, working directory, staging area, commit
 * - Branching and merging (git branch, git merge, git rebase)
 * - Remote repositories (git push, git pull, git fetch)
 * - Git workflows (GitFlow, trunk-based development)
 * - .gitignore file to exclude build artifacts
 * 
 * Hint:
 * - git init creates a new repository
 * - git add stages changes, git commit creates a commit
 * - git branch creates branches for parallel development
 * - git merge combines branches, git rebase rewrites history
 * - git pull = git fetch + git merge
 * 
 * Expected Output (Solution):
 * Git Workflow:
 * 1. git init / git clone
 * 2. git add (stage changes)
 * 3. git commit (create commit)
 * 4. git push (upload to remote)
 * 5. git pull (download from remote)
 * Branching: git branch feature, git checkout feature
 * Merging: git merge feature (onto main)
 */
public class Challenge {

}